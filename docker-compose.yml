services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: nasa_rag
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - nasa_rag_pgdata:/var/lib/postgresql/data

volumes:
  nasa_rag_pgdata:
